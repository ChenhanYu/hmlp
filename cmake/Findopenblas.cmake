ADD_LIBRARY(openblas SHARED IMPORTED)
FIND_LIBRARY(openblas_LIBRARY_PATH openblas HINTS "$ENV{OPRNBLASROOT}")
IF(openblas_LIBRARY_PATH)
    MESSAGE("cudalibmg found ${openblas_LIBRARY_PATH}")
ELSE()
    MESSAGE(STATUS $ENV{OPENBLASROOT})
    MESSAGE(FATAL_ERROR "[FATAL ERROR] must export OPENBLASROOT='path/to/openblas/root'.")
ENDIF()

SET_TARGET_PROPERTIES(openblas PROPERTIES IMPORTED_LOCATION "${openblas_LIBRARY_PATH}")
